# Thermostat Python Utilities

This directory contains common Python utilities for Thermostat 
like output channel setup.

To install this module locally (in editable mode):
```
python -m pip install -e .
```
